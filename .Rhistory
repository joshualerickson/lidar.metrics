mean_closeness = tryCatch(mean(igraph::closeness(g, weights = E(g)$inv_weight), na.rm = TRUE), error = function(e) 0),
n_components = comps$no,
avg_path_length = tryCatch({
d <- igraph::distances(g, weights = E(g)$inv_weight)
mean(d[is.finite(d) & d > 0], na.rm = TRUE)
}, error = function(e) 0),
eigen_ratio = tryCatch({
pca <- prcomp(coords)
pca$sdev[1] / pca$sdev[2]
}, error = function(e) 0),
graph_density = tryCatch(igraph::edge_density(g), error = function(e) 0)
)
return(results)
}
testing <- pixel_metrics(ctg_norm_clip,~connectivity_metrics_binned(X,Y,Z,
voxel_res = 3,
edge_thresh_values = c(3,3),
z_40 = 12.1),
res=30)
testing
testing
load_graph_deps
lidar.metrics::load_graph_deps
load_graph_deps <- function ()
{
pkgs <- c("lidR", "dbscan", "igraph", "dplyr", "tibble",
"purrr", "stats")
for (pkg in pkgs) {
suppressMessages(require(pkg, character.only = TRUE))
}
invisible(TRUE)
}
#' metrics such as weighted degree, betweenness, closeness, and component structure.
#'
#' @param las A LAS object (point cloud) from the `lidR` package.
#' @param z_min Minimum Z height threshold.
#' @param z_max Maximum Z height threshold.
#' @param edge_thresh Numeric, distance threshold for creating edges.
#' @param voxel_res Numeric, XY voxel resolution.
#'
#' @return A named list of graph-theoretic metrics.
#' @export
compute_graph_metrics <- function(las, z_min, z_max, edge_thresh, voxel_res) {
load_graph_deps()
density <- as.numeric(length(las@data$Z)) / (30 * 30)  # if using 30 m pixels
if (density > 6.5) {
las <- lidR::decimate_points(las, lidR::random_per_voxel(res = 3, n = 10))
}
las_filtered <- lidR::filter_poi(las, Z > z_min & Z <= z_max)
if (is.empty(las_filtered) || length(las_filtered@data$Z) < 2) {
return(named_zero_metrics())
}
voxel_df <- lidR::voxel_metrics(
las_filtered,
func = ~list(point_count = length(Z)),
res = voxel_res
)
voxel_df <- as.data.frame(voxel_df)
required_cols <- c("X", "Y", "Z", "point_count")
if (!all(required_cols %in% names(voxel_df))) {
return(named_zero_metrics())
}
idx <- which(voxel_df$Z > z_min & voxel_df$Z <= z_max)
if (length(idx) < 3) return(named_zero_metrics())
coords <- voxel_df[idx, c("X", "Y", "Z")]
coords <- as.matrix(data.frame(lapply(coords, as.numeric)))
pt_counts <- voxel_df$point_count[idx]
nn <- dbscan::frNN(coords, eps = edge_thresh)
edges <- dplyr::tibble(
from = rep(seq_along(nn$id), lengths(nn$id)),
to = unlist(nn$id)
) %>%
dplyr::filter(from != to, from < to) %>%
dplyr::mutate(weight = pt_counts[from] + pt_counts[to])
g <- igraph::graph_from_data_frame(edges, directed = FALSE)
E(g)$weight <- edges$weight
E(g)$inv_weight <- 1 / pmax(E(g)$weight, 1e-6)
comps <- igraph::components(g)
results <- list(
mean_weighted_degree = mean(igraph::strength(g, weights = E(g)$inv_weight), na.rm = TRUE),
mean_betweenness = tryCatch(mean(igraph::betweenness(g, weights = E(g)$inv_weight), na.rm = TRUE), error = function(e) 0),
mean_closeness = tryCatch(mean(igraph::closeness(g, weights = E(g)$inv_weight), na.rm = TRUE), error = function(e) 0),
n_components = comps$no,
avg_path_length = tryCatch({
d <- igraph::distances(g, weights = E(g)$inv_weight)
mean(d[is.finite(d) & d > 0], na.rm = TRUE)
}, error = function(e) 0),
eigen_ratio = tryCatch({
pca <- prcomp(coords)
pca$sdev[1] / pca$sdev[2]
}, error = function(e) 0),
graph_density = tryCatch(igraph::edge_density(g), error = function(e) 0)
)
return(results)
}
testing <- pixel_metrics(ctg_norm_clip,~connectivity_metrics_binned(X,Y,Z,
voxel_res = 3,
edge_thresh_values = c(3,3),
z_40 = 12.1),
res=30)
testing
testing
plot(testing[[1:5]])
#' metrics such as weighted degree, betweenness, closeness, and component structure.
#'
#' @param las A LAS object (point cloud) from the `lidR` package.
#' @param z_min Minimum Z height threshold.
#' @param z_max Maximum Z height threshold.
#' @param edge_thresh Numeric, distance threshold for creating edges.
#' @param voxel_res Numeric, XY voxel resolution.
#'
#' @return A named list of graph-theoretic metrics.
#' @export
compute_graph_metrics <- function(las, z_min, z_max, edge_thresh, voxel_res) {
load_graph_deps()
density <- as.numeric(length(las@data$Z)) / (30 * 30)  # if using 30 m pixels
if (density > 3.5) {
las <- lidR::decimate_points(las, lidR::random_per_voxel(res = 3, n = 10))
}
las_filtered <- lidR::filter_poi(las, Z > z_min & Z <= z_max)
if (is.empty(las_filtered) || length(las_filtered@data$Z) < 2) {
return(named_zero_metrics())
}
voxel_df <- lidR::voxel_metrics(
las_filtered,
func = ~list(point_count = length(Z)),
res = voxel_res
)
voxel_df <- as.data.frame(voxel_df)
required_cols <- c("X", "Y", "Z", "point_count")
if (!all(required_cols %in% names(voxel_df))) {
return(named_zero_metrics())
}
idx <- which(voxel_df$Z > z_min & voxel_df$Z <= z_max)
if (length(idx) < 3) return(named_zero_metrics())
coords <- voxel_df[idx, c("X", "Y", "Z")]
coords <- as.matrix(data.frame(lapply(coords, as.numeric)))
pt_counts <- voxel_df$point_count[idx]
nn <- dbscan::frNN(coords, eps = edge_thresh)
edges <- dplyr::tibble(
from = rep(seq_along(nn$id), lengths(nn$id)),
to = unlist(nn$id)
) %>%
dplyr::filter(from != to, from < to) %>%
dplyr::mutate(weight = pt_counts[from] + pt_counts[to])
g <- igraph::graph_from_data_frame(edges, directed = FALSE)
E(g)$weight <- edges$weight
E(g)$inv_weight <- 1 / pmax(E(g)$weight, 1e-6)
comps <- igraph::components(g)
results <- list(
mean_weighted_degree = mean(igraph::strength(g, weights = E(g)$inv_weight), na.rm = TRUE),
mean_betweenness = tryCatch(mean(igraph::betweenness(g, weights = E(g)$inv_weight), na.rm = TRUE), error = function(e) 0),
mean_closeness = tryCatch(mean(igraph::closeness(g, weights = E(g)$inv_weight), na.rm = TRUE), error = function(e) 0),
n_components = comps$no,
avg_path_length = tryCatch({
d <- igraph::distances(g, weights = E(g)$inv_weight)
mean(d[is.finite(d) & d > 0], na.rm = TRUE)
}, error = function(e) 0),
eigen_ratio = tryCatch({
pca <- prcomp(coords)
pca$sdev[1] / pca$sdev[2]
}, error = function(e) 0),
graph_density = tryCatch(igraph::edge_density(g), error = function(e) 0)
)
return(results)
}
testing <- pixel_metrics(ctg_norm_clip,~connectivity_metrics_binned(X,Y,Z,
voxel_res = 3,
edge_thresh_values = c(3,3),
z_40 = 12.1),
res=30)
testing
testing
plot(testing[[1:5]])
plot(testing[[6:10]])
plot(testing[[6:11]])
#' metrics such as weighted degree, betweenness, closeness, and component structure.
#'
#' @param las A LAS object (point cloud) from the `lidR` package.
#' @param z_min Minimum Z height threshold.
#' @param z_max Maximum Z height threshold.
#' @param edge_thresh Numeric, distance threshold for creating edges.
#' @param voxel_res Numeric, XY voxel resolution.
#'
#' @return A named list of graph-theoretic metrics.
#' @export
compute_graph_metrics <- function(las, z_min, z_max, edge_thresh, voxel_res) {
load_graph_deps()
density <- as.numeric(length(las@data$Z)) / (30 * 30)  # if using 30 m pixels
if (density > 2) {
las <- lidR::decimate_points(las, lidR::random_per_voxel(res = 3, n = 10))
}
las_filtered <- lidR::filter_poi(las, Z > z_min & Z <= z_max)
if (is.empty(las_filtered) || length(las_filtered@data$Z) < 2) {
return(named_zero_metrics())
}
voxel_df <- lidR::voxel_metrics(
las_filtered,
func = ~list(point_count = length(Z)),
res = voxel_res
)
voxel_df <- as.data.frame(voxel_df)
required_cols <- c("X", "Y", "Z", "point_count")
if (!all(required_cols %in% names(voxel_df))) {
return(named_zero_metrics())
}
idx <- which(voxel_df$Z > z_min & voxel_df$Z <= z_max)
if (length(idx) < 3) return(named_zero_metrics())
coords <- voxel_df[idx, c("X", "Y", "Z")]
coords <- as.matrix(data.frame(lapply(coords, as.numeric)))
pt_counts <- voxel_df$point_count[idx]
nn <- dbscan::frNN(coords, eps = edge_thresh)
edges <- dplyr::tibble(
from = rep(seq_along(nn$id), lengths(nn$id)),
to = unlist(nn$id)
) %>%
dplyr::filter(from != to, from < to) %>%
dplyr::mutate(weight = pt_counts[from] + pt_counts[to])
g <- igraph::graph_from_data_frame(edges, directed = FALSE)
E(g)$weight <- edges$weight
E(g)$inv_weight <- 1 / pmax(E(g)$weight, 1e-6)
comps <- igraph::components(g)
results <- list(
mean_weighted_degree = mean(igraph::strength(g, weights = E(g)$inv_weight), na.rm = TRUE),
mean_betweenness = tryCatch(mean(igraph::betweenness(g, weights = E(g)$inv_weight), na.rm = TRUE), error = function(e) 0),
mean_closeness = tryCatch(mean(igraph::closeness(g, weights = E(g)$inv_weight), na.rm = TRUE), error = function(e) 0),
n_components = comps$no,
avg_path_length = tryCatch({
d <- igraph::distances(g, weights = E(g)$inv_weight)
mean(d[is.finite(d) & d > 0], na.rm = TRUE)
}, error = function(e) 0),
eigen_ratio = tryCatch({
pca <- prcomp(coords)
pca$sdev[1] / pca$sdev[2]
}, error = function(e) 0),
graph_density = tryCatch(igraph::edge_density(g), error = function(e) 0)
)
return(results)
}
testing <- pixel_metrics(ctg_norm_clip,~connectivity_metrics_binned(X,Y,Z,
voxel_res = 3,
edge_thresh_values = c(3,3),
z_40 = 12.1),
res=30)
plot(testing[[1:5]])
opt_filter(ctg_norm_clip) <- paste(
#"-keep_class 1",              # only keep class 1 (unclassified, typically vegetation)
"-drop_class 7 9",            # drop noise and water
"-drop_withheld",            # drop withheld (often invalid points)
"-drop_z_below 0.3",         # avoid ground clutter
"-drop_z_above 6.1",        # truncate canopy top
#"-drop_intensity_below 2",  # low signal = noise
"-drop_overlap",
#"-keep_return 1",       # keep only first returns
"-drop_scan_angle_above 25",  # limit to near-nadir points
"-drop_scan_angle_below -25"  # limit to near-nadir points
)
testing <- pixel_metrics(ctg_norm_clip,~list(density = length(Z) / (30 * 30)),
res=30)
plot(testing)
plot(testing<1.5)
plot(testing<1)
plot(testing)
plot(testing<1)
plot(testing)
plot(testing<1)
plot(testing)
plot(testing>5)
plot(testing>3)
plot(testing>4)
opt_filter(ctg_norm_clip) <- paste(
#"-keep_class 1",              # only keep class 1 (unclassified, typically vegetation)
"-drop_class 7 9",            # drop noise and water
"-drop_withheld",            # drop withheld (often invalid points)
"-drop_z_below 0.3",         # avoid ground clutter
"-drop_z_above 12.1",        # truncate canopy top
#"-drop_intensity_below 2",  # low signal = noise
"-drop_overlap",
#"-keep_return 1",       # keep only first returns
"-drop_scan_angle_above 25",  # limit to near-nadir points
"-drop_scan_angle_below -25"  # limit to near-nadir points
)
opt_filter(ctg_norm_clip) <- paste(
#"-keep_class 1",              # only keep class 1 (unclassified, typically vegetation)
"-drop_class 7 9",            # drop noise and water
"-drop_withheld",            # drop withheld (often invalid points)
"-drop_z_below 0.3",         # avoid ground clutter
"-drop_z_above 12.1",        # truncate canopy top
#"-drop_intensity_below 2",  # low signal = noise
"-drop_overlap",
#"-keep_return 1",       # keep only first returns
"-drop_scan_angle_above 25",  # limit to near-nadir points
"-drop_scan_angle_below -25"  # limit to near-nadir points
)
#' metrics such as weighted degree, betweenness, closeness, and component structure.
#'
#' @param las A LAS object (point cloud) from the `lidR` package.
#' @param z_min Minimum Z height threshold.
#' @param z_max Maximum Z height threshold.
#' @param edge_thresh Numeric, distance threshold for creating edges.
#' @param voxel_res Numeric, XY voxel resolution.
#'
#' @return A named list of graph-theoretic metrics.
#' @export
compute_graph_metrics <- function(las, z_min, z_max, edge_thresh, voxel_res) {
load_graph_deps()
density <- length(las@data$Z) / (30 * 30)  # if using 30 m pixels
las_filtered <- lidR::filter_poi(las, Z > z_min & Z <= z_max)
if (density > 4) {
las_filtered <- lidR::decimate_points(las_filtered, lidR::random_per_voxel(res = 3, n = 10))
}
if (is.empty(las_filtered) || length(las_filtered@data$Z) < 2) {
return(named_zero_metrics())
}
voxel_df <- lidR::voxel_metrics(
las_filtered,
func = ~list(point_count = length(Z)),
res = voxel_res
)
voxel_df <- as.data.frame(voxel_df)
required_cols <- c("X", "Y", "Z", "point_count")
if (!all(required_cols %in% names(voxel_df))) {
return(named_zero_metrics())
}
idx <- which(voxel_df$Z > z_min & voxel_df$Z <= z_max)
if (length(idx) < 3) return(named_zero_metrics())
coords <- voxel_df[idx, c("X", "Y", "Z")]
coords <- as.matrix(data.frame(lapply(coords, as.numeric)))
pt_counts <- voxel_df$point_count[idx]
nn <- dbscan::frNN(coords, eps = edge_thresh)
edges <- dplyr::tibble(
from = rep(seq_along(nn$id), lengths(nn$id)),
to = unlist(nn$id)
) %>%
dplyr::filter(from != to, from < to) %>%
dplyr::mutate(weight = pt_counts[from] + pt_counts[to])
g <- igraph::graph_from_data_frame(edges, directed = FALSE)
E(g)$weight <- edges$weight
E(g)$inv_weight <- 1 / pmax(E(g)$weight, 1e-6)
comps <- igraph::components(g)
results <- list(
mean_weighted_degree = mean(igraph::strength(g, weights = E(g)$inv_weight), na.rm = TRUE),
mean_betweenness = tryCatch(mean(igraph::betweenness(g, weights = E(g)$inv_weight), na.rm = TRUE), error = function(e) 0),
mean_closeness = tryCatch(mean(igraph::closeness(g, weights = E(g)$inv_weight), na.rm = TRUE), error = function(e) 0),
n_components = comps$no,
avg_path_length = tryCatch({
d <- igraph::distances(g, weights = E(g)$inv_weight)
mean(d[is.finite(d) & d > 0], na.rm = TRUE)
}, error = function(e) 0),
eigen_ratio = tryCatch({
pca <- prcomp(coords)
pca$sdev[1] / pca$sdev[2]
}, error = function(e) 0),
graph_density = tryCatch(igraph::edge_density(g), error = function(e) 0)
)
return(results)
}
testing <- pixel_metrics(ctg_norm_clip,~connectivity_metrics_binned(X,Y,Z,
voxel_res = 3,
edge_thresh_values = c(3,3),
z_40 = 12.1),
res=30)
plot(testing[[1:5]])
#' metrics such as weighted degree, betweenness, closeness, and component structure.
#'
#' @param las A LAS object (point cloud) from the `lidR` package.
#' @param z_min Minimum Z height threshold.
#' @param z_max Maximum Z height threshold.
#' @param edge_thresh Numeric, distance threshold for creating edges.
#' @param voxel_res Numeric, XY voxel resolution.
#'
#' @return A named list of graph-theoretic metrics.
#' @export
compute_graph_metrics <- function(las, z_min, z_max, edge_thresh, voxel_res) {
load_graph_deps()
density <- length(las@data$Z) / (30 * 30)  # if using 30 m pixels
las_filtered <- lidR::filter_poi(las, Z > z_min & Z <= z_max)
if (density > 4) {
las_filtered <- lidR::decimate_points(las_filtered, lidR::random_per_voxel(res = 3, n = 5))
}
if (is.empty(las_filtered) || length(las_filtered@data$Z) < 2) {
return(named_zero_metrics())
}
voxel_df <- lidR::voxel_metrics(
las_filtered,
func = ~list(point_count = length(Z)),
res = voxel_res
)
voxel_df <- as.data.frame(voxel_df)
required_cols <- c("X", "Y", "Z", "point_count")
if (!all(required_cols %in% names(voxel_df))) {
return(named_zero_metrics())
}
idx <- which(voxel_df$Z > z_min & voxel_df$Z <= z_max)
if (length(idx) < 3) return(named_zero_metrics())
coords <- voxel_df[idx, c("X", "Y", "Z")]
coords <- as.matrix(data.frame(lapply(coords, as.numeric)))
pt_counts <- voxel_df$point_count[idx]
nn <- dbscan::frNN(coords, eps = edge_thresh)
edges <- dplyr::tibble(
from = rep(seq_along(nn$id), lengths(nn$id)),
to = unlist(nn$id)
) %>%
dplyr::filter(from != to, from < to) %>%
dplyr::mutate(weight = pt_counts[from] + pt_counts[to])
g <- igraph::graph_from_data_frame(edges, directed = FALSE)
E(g)$weight <- edges$weight
E(g)$inv_weight <- 1 / pmax(E(g)$weight, 1e-6)
comps <- igraph::components(g)
results <- list(
mean_weighted_degree = mean(igraph::strength(g, weights = E(g)$inv_weight), na.rm = TRUE),
mean_betweenness = tryCatch(mean(igraph::betweenness(g, weights = E(g)$inv_weight), na.rm = TRUE), error = function(e) 0),
mean_closeness = tryCatch(mean(igraph::closeness(g, weights = E(g)$inv_weight), na.rm = TRUE), error = function(e) 0),
n_components = comps$no,
avg_path_length = tryCatch({
d <- igraph::distances(g, weights = E(g)$inv_weight)
mean(d[is.finite(d) & d > 0], na.rm = TRUE)
}, error = function(e) 0),
eigen_ratio = tryCatch({
pca <- prcomp(coords)
pca$sdev[1] / pca$sdev[2]
}, error = function(e) 0),
graph_density = tryCatch(igraph::edge_density(g), error = function(e) 0)
)
return(results)
}
testing <- pixel_metrics(ctg_norm_clip,~connectivity_metrics_binned(X,Y,Z,
voxel_res = 3,
edge_thresh_values = c(3,3),
z_40 = 12.1),
res=30)
plot(testing[[1:5]])
#' metrics such as weighted degree, betweenness, closeness, and component structure.
#'
#' @param las A LAS object (point cloud) from the `lidR` package.
#' @param z_min Minimum Z height threshold.
#' @param z_max Maximum Z height threshold.
#' @param edge_thresh Numeric, distance threshold for creating edges.
#' @param voxel_res Numeric, XY voxel resolution.
#'
#' @return A named list of graph-theoretic metrics.
#' @export
compute_graph_metrics <- function(las, z_min, z_max, edge_thresh, voxel_res) {
load_graph_deps()
density <- length(las@data$Z) / (30 * 30)  # if using 30 m pixels
las_filtered <- lidR::filter_poi(las, Z > z_min & Z <= z_max)
if (density > 4) {
las_filtered <- lidR::decimate_points(las_filtered, lidR::homogenize(res = 1, density = 2))
}
if (is.empty(las_filtered) || length(las_filtered@data$Z) < 2) {
return(named_zero_metrics())
}
voxel_df <- lidR::voxel_metrics(
las_filtered,
func = ~list(point_count = length(Z)),
res = voxel_res
)
voxel_df <- as.data.frame(voxel_df)
required_cols <- c("X", "Y", "Z", "point_count")
if (!all(required_cols %in% names(voxel_df))) {
return(named_zero_metrics())
}
idx <- which(voxel_df$Z > z_min & voxel_df$Z <= z_max)
if (length(idx) < 3) return(named_zero_metrics())
coords <- voxel_df[idx, c("X", "Y", "Z")]
coords <- as.matrix(data.frame(lapply(coords, as.numeric)))
pt_counts <- voxel_df$point_count[idx]
nn <- dbscan::frNN(coords, eps = edge_thresh)
edges <- dplyr::tibble(
from = rep(seq_along(nn$id), lengths(nn$id)),
to = unlist(nn$id)
) %>%
dplyr::filter(from != to, from < to) %>%
dplyr::mutate(weight = pt_counts[from] + pt_counts[to])
g <- igraph::graph_from_data_frame(edges, directed = FALSE)
E(g)$weight <- edges$weight
E(g)$inv_weight <- 1 / pmax(E(g)$weight, 1e-6)
comps <- igraph::components(g)
results <- list(
mean_weighted_degree = mean(igraph::strength(g, weights = E(g)$inv_weight), na.rm = TRUE),
mean_betweenness = tryCatch(mean(igraph::betweenness(g, weights = E(g)$inv_weight), na.rm = TRUE), error = function(e) 0),
mean_closeness = tryCatch(mean(igraph::closeness(g, weights = E(g)$inv_weight), na.rm = TRUE), error = function(e) 0),
n_components = comps$no,
avg_path_length = tryCatch({
d <- igraph::distances(g, weights = E(g)$inv_weight)
mean(d[is.finite(d) & d > 0], na.rm = TRUE)
}, error = function(e) 0),
eigen_ratio = tryCatch({
pca <- prcomp(coords)
pca$sdev[1] / pca$sdev[2]
}, error = function(e) 0),
graph_density = tryCatch(igraph::edge_density(g), error = function(e) 0)
)
return(results)
}
testing <- pixel_metrics(ctg_norm_clip,~connectivity_metrics_binned(X,Y,Z,
voxel_res = 3,
edge_thresh_values = c(3,3),
z_40 = 12.1),
res=30)
plot(testing[[1:5]])
testing_strata <- pixel_metrics(ctg_norm_clip,~lidar.metrics::StrataMetrics(Z,ReturnNumber),
res=30)
ctg_norm_clip@data
ctg_norm_clip@data$filename
ctg_norm_clip@data[1,]$filename
basename(ctg_norm_clip@data[1,]$filename)
map(ctg_norm_clip@data$filename, ~writeLAS(.x, paste0('/home/josh.erickson/Documents/projects/lidar/testing/', basename(.x))))
purrr::map(ctg_norm_clip@data$filename, ~writeLAS(.x, paste0('/home/josh.erickson/Documents/projects/lidar/testing/', basename(.x))))
purrr::map(ctg_norm_clip@data$filename, ~file.copy(.x, paste0('/home/josh.erickson/Documents/projects/lidar/testing/')))
lidar.metrics::save_crs_to_folder
lidar.metrics::save_crs_to_folder(st_crs(ctg_norm_clip), out_dir = '/mnt/nvmeDrive/data/LIDAR/MT_Statewide_P3_5_B21/')
