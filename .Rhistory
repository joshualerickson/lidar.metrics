mean_degree = 0,
mean_betweenness = 0,
mean_closeness = NA_real_,
n_components = NA_real_,
avg_path_length = NA_real_,
eigen_ratio = NA_real_,
graph_density = 0
))
} else if (type == 'canopy') {
return(list(
fractional_canopy_cover = NA_real_,
LAI = NA_real_,
LAD_max = NA_real_,
LAD_mean = NA_real_,
LAD_z_max = NA_real_
))
} else if (type == 'trees') {
return(list(
n_trees = 0,
trees_per_acre = 0,
n_low = 0,
n_low_mid = 0,
n_mid = 0,
n_mid_upper = 0,
n_upper = 0,
n_0_6_1 = 0,
n_0_12_1 = 0,
n_0_24_1 = 0,
n_0_36_1 = 0,
n_0_Inf = 0,
topo_residual_sd = NA_real_,
topo_entropy = NA_real_
))
} else {
stop("Invalid type. Must be 'graph' or 'canopy'.")
}
}
wf <- function(x) {x*0.17 + 3}
testing_trees <- pixel_metrics(ctg_norm_clip[1:2,],~tree_detection(X,Y,Z,
window_func = wf),
res=30)
plot(testing_trees)
testing_trees <- pixel_metrics(ctg_norm_clip[1:2,],~tree_detection(X,Y,Z,
window_func =  function(x) {
if(x <= 6) {
w <- log1p(x) * 1.5
} else {
w <- x * 0.17 + 3
}
return(w)
}),
res=30)
testing_trees <- pixel_metrics(ctg_norm_clip[1:2,],~tree_detection(X,Y,Z,
window_func =  function(x) {
if(x <= 6) {
w <- log1p(x) * 1.5
} else {
w <- x * 0.17 + 3
}
}),
res=30)
plot(testing_trees)
testing_trees <- pixel_metrics(ctg_norm_clip[1:2,],~tree_detection(X,Y,Z,
window_func =  function(x) {
if(x <= 6) {
log1p(x) * 1.5
} else {
x * 0.17 + 3
}
}),
res=30)
plot(testing_trees)
wf <- function(x) {
ifelse(x <= 6, log1p(x) * 1.5, x * 0.17 + 3)
}
testing_trees <- pixel_metrics(ctg_norm_clip[1:2,],~tree_detection(X,Y,Z,
window_func =  wf),
res=30)
plot(testing_trees)
testing_trees <- pixel_metrics(ctg_norm_clip[1:2,],~tree_detection(X,Y,Z,
window_func =
function(x) ifelse(x <= 6, log1p(x) * 1.5, x * 0.17 + 3)),
res=30)
plot(testing_trees)
}
wf <- function(x) {
tryCatch({ifelse(x <= 6, log1p(x) * 1.5, x * 0.17 + 3)}, error = function(e) return(1))
}
testing_trees <- pixel_metrics(ctg_norm_clip[1:2,],~tree_detection(X,Y,Z,
window_func =wf),
res=30)
plot(testing_trees)
wf <- function(x) {ifelse(x<6,x*0.17 + 3, x + 1)}
testing_trees <- pixel_metrics(ctg_norm_clip[1:2,],~tree_detection(X,Y,Z,
window_func =wf),
res=30)
wf <- function(x) {
w <- pmax(x, 1)
if (w <= 6) {
w <- log1p(x) * 1.5
} else {
w <- x * 0.17 + 3
}
return(w)
}
plot(testing_trees)
log1p(0.4) * 1.5
log1p(0.4)
wf <- function(x) {
w <- pmax(x, 1)
if (w <= 6) {
w <- log1p(w) * 1.5
} else {
w <- w * 0.17 + 3
}
return(w)
}
testing_trees <- pixel_metrics(ctg_norm_clip[1:2,],~tree_detection(X,Y,Z,
window_func =wf),
res=30)
plot(testing_trees)
wf <- function(x) {
w <- pmax(x, 1)
if (w < 6) {
w <- log1p(w) * 1.5
} else {
w <- w * 0.17 + 3
}
return(w)
}
testing_trees <- pixel_metrics(ctg_norm_clip[1:2,],~tree_detection(X,Y,Z,
window_func =wf),
res=30)
plot(testing_trees)
0.3:5
0.3:6
0.3:6*0.5
0.3:6*0.75
0.3:6*0.25
0.3:6*0.25 + 1
wf <- function(x) {ifelse(x<6,x * 0.25 + 1,x*0.17 + 3)}
testing_trees <- pixel_metrics(ctg_norm_clip[1:2,],~tree_detection(X,Y,Z,
window_func =wf),
res=30)
plot(testing_trees)
testing_trees <- pixel_metrics(ctg_norm_clip,~tree_detection(X,Y,Z,
window_func =wf),
res=30)
#' @param z Numeric vector of Z (height) values.
#' @param window_func Function that returns a window size for tree detection.
#'
#' @return A named list of tree metrics:
#' \itemize{
#'   \item \code{n_trees}, \code{trees_per_acre}
#'   \item \code{n_low}, \code{n_low_mid}, \code{n_mid}, \code{n_mid_upper}, \code{n_upper}
#'   \item \code{topo_residual_sd}, \code{topo_entropy}
#' }
#' @export
tree_detection <- function(x, y, z,
window_func = function(x) {
w <- pmax(x, 1)
w <- log1p(w) * 1.5 + 2
w <- pmin(w, 6)
w[is.na(w) | w <= 0] <- 3
return(w)
}
) {
load_graph_deps()
tryCatch({
if (length(x) < 5 || length(z) < 5) {
return(named_zero_metrics('trees'))
}
las_data <- suppressMessages(LAS(data.frame(X = x, Y = y, Z = z)))
trees <- tryCatch({
lidR::locate_trees(las_data, lidR::lmf(ws = window_func))
}, error = function(e) {
return(named_zero_metrics('trees'))
})
if (is.null(trees) || nrow(trees) == 0) {
return(named_zero_metrics('trees'))
}
coords <- sf::st_coordinates(trees)
z_vals <- trees$Z
# True cumulative-from-0.3m counts
n_0_6_1     <- sum(z_vals >= 0.3 & z_vals < 6.1)
n_0_12_1    <- sum(z_vals >= 0.3 & z_vals < 12.1)
n_0_24_1    <- sum(z_vals >= 0.3 & z_vals < 24.1)
n_0_36_1    <- sum(z_vals >= 0.3 & z_vals < 36.1)
n_0_Inf     <- sum(z_vals >= 0.3)
zbin <- cut(z_vals,
breaks = c(0.3, 6.1, 12.1, 24.1, 36.1, Inf),
labels = c("low", "low_mid", "mid", "mid_upper", "upper"))
strata <- c("low", "low_mid", "mid", "mid_upper", "upper")
strata_table <- table(zbin)
strata_counts <- setNames(rep(0, length(strata)), strata)
strata_counts[names(strata_table)] <- as.integer(strata_table)
df <- data.frame(x = coords[, 1], y = coords[, 2], z = z_vals)
residual_sd <- NA_real_
topo_entropy <- NA_real_
if (nrow(df) >= 5) {
tryCatch({
fit <- lm(z ~ splines::bs(x, df = 4) + splines::bs(y, df = 4), data = df)
residuals <- df$z - predict(fit, newdata = df)
residual_sd <- sd(residuals)
h <- hist(residuals, breaks = 10, plot = FALSE)
p <- h$counts / sum(h$counts)
topo_entropy <- -sum(p * log(p + 1e-10))
}, error = function(e) {return(NA_real_)
})
}
n_trees <- nrow(trees)
trees_per_acre <- n_trees / 0.222395
return(list(
n_trees = as.numeric(n_trees),
trees_per_acre = as.numeric(trees_per_acre),
n_low = strata_counts["low"],
n_low_mid = strata_counts["low_mid"],
n_mid = strata_counts["mid"],
n_mid_upper = strata_counts["mid_upper"],
n_upper = strata_counts["upper"],
n_0_6_1 = n_0_6_1,
n_0_12_1 = n_0_12_1,
n_0_24_1 = n_0_24_1,
n_0_36_1 = n_0_36_1,
n_0_Inf = n_0_Inf,
topo_residual_sd = residual_sd,
topo_entropy = topo_entropy
))
}, error = function(e) {
return(named_zero_metrics('trees'))
})
}
testing_trees <- pixel_metrics(ctg_norm_clip,~tree_detection(X,Y,Z,
window_func =wf),
res=30)
#' @param z Numeric vector of Z (height) values.
#' @param window_func Function that returns a window size for tree detection.
#'
#' @return A named list of tree metrics:
#' \itemize{
#'   \item \code{n_trees}, \code{trees_per_acre}
#'   \item \code{n_low}, \code{n_low_mid}, \code{n_mid}, \code{n_mid_upper}, \code{n_upper}
#'   \item \code{topo_residual_sd}, \code{topo_entropy}
#' }
#' @export
tree_detection <- function(x, y, z,
window_func = function(x) {
w <- pmax(x, 1)
w <- log1p(w) * 1.5 + 2
w <- pmin(w, 6)
w[is.na(w) | w <= 0] <- 3
return(w)
}
) {
load_graph_deps()
tryCatch({
if (length(x) < 5 || length(z) < 5) {
return(named_zero_metrics('trees'))
}
las_data <- suppressMessages(LAS(data.frame(X = x, Y = y, Z = z)))
trees <- tryCatch({
lidR::locate_trees(las_data, lidR::lmf(ws = window_func))
}, error = function(e) {
return(named_zero_metrics('trees'))
})
if (is.null(trees) || nrow(trees) == 0) {
return(named_zero_metrics('trees'))
}
coords <- sf::st_coordinates(trees)
z_vals <- trees$Z
# True cumulative-from-0.3m counts
n_0_6_1     <- as.numeric(sum(z_vals >= 0.3 & z_vals < 6.1))
n_0_12_1    <- as.numeric(sum(z_vals >= 0.3 & z_vals < 12.1))
n_0_24_1    <- as.numeric(sum(z_vals >= 0.3 & z_vals < 24.1))
n_0_36_1    <- as.numeric(sum(z_vals >= 0.3 & z_vals < 36.1))
n_0_Inf     <- as.numeric(sum(z_vals >= 0.3))
zbin <- cut(z_vals,
breaks = c(0.3, 6.1, 12.1, 24.1, 36.1, Inf),
labels = c("low", "low_mid", "mid", "mid_upper", "upper"))
strata <- c("low", "low_mid", "mid", "mid_upper", "upper")
strata_table <- table(zbin)
strata_counts <- setNames(rep(0, length(strata)), strata)
strata_counts[names(strata_table)] <- as.numeric(strata_table)
df <- data.frame(x = coords[, 1], y = coords[, 2], z = z_vals)
residual_sd <- NA_real_
topo_entropy <- NA_real_
if (nrow(df) >= 5) {
tryCatch({
fit <- lm(z ~ splines::bs(x, df = 4) + splines::bs(y, df = 4), data = df)
residuals <- df$z - predict(fit, newdata = df)
residual_sd <- sd(residuals)
h <- hist(residuals, breaks = 10, plot = FALSE)
p <- h$counts / sum(h$counts)
topo_entropy <- -sum(p * log(p + 1e-10))
}, error = function(e) {return(NA_real_)
})
}
n_trees <- nrow(trees)
trees_per_acre <- n_trees / 0.222395
return(list(
n_trees = as.numeric(n_trees),
trees_per_acre = as.numeric(trees_per_acre),
n_low = strata_counts["low"],
n_low_mid = strata_counts["low_mid"],
n_mid = strata_counts["mid"],
n_mid_upper = strata_counts["mid_upper"],
n_upper = strata_counts["upper"],
n_0_6_1 = n_0_6_1,
n_0_12_1 = n_0_12_1,
n_0_24_1 = n_0_24_1,
n_0_36_1 = n_0_36_1,
n_0_Inf = n_0_Inf,
topo_residual_sd = residual_sd,
topo_entropy = topo_entropy
))
}, error = function(e) {
return(named_zero_metrics('trees'))
})
}
testing_trees <- pixel_metrics(ctg_norm_clip,~tree_detection(X,Y,Z,
window_func =wf),
res=30)
plot(testing_trees)
#' Return a List of Named NA Metrics for Zero-Point Tiles
#'
#' Provides default NA values for graph or canopy metrics when input data is insufficient.
#'
#' @param type Character string, either `"graph"`, `"canopy"` or `"trees"`. Determines which metric structure to return.
#'
#' @return A named list of metrics filled with `NA_real_` values appropriate to the type.
#' @export
named_zero_metrics <- function(type = 'graph') {
if (type == 'graph') {
return(list(
mean_degree = 0,
mean_betweenness = 0,
mean_closeness = NA_real_,
n_components = NA_real_,
avg_path_length = NA_real_,
eigen_ratio = NA_real_,
graph_density = 0
))
} else if (type == 'canopy') {
return(list(
fractional_canopy_cover = NA_real_,
LAI = NA_real_,
LAD_max = NA_real_,
LAD_mean = NA_real_,
LAD_z_max = NA_real_
))
} else if (type == 'trees') {
return(list(
n_trees = 0,
trees_per_acre = 0,
n_low = 0,
n_low_mid = 0,
n_mid = 0,
n_mid_upper = 0,
n_upper = 0,
n_0_6_1 = 0,
n_0_12_1 = 0,
n_0_24_1 = 0,
n_0_36_1 = 0,
n_0_Inf = 0,
n_gt_6_1 = 0,
n_gt_12_1 = 0,
n_gt_24_1 = 0,
n_gt_36_1 = 0,
topo_residual_sd = NA_real_,
topo_entropy = NA_real_
))
} else {
stop("Invalid type. Must be 'graph' or 'canopy'.")
}
}
#' @param z Numeric vector of Z (height) values.
#' @param window_func Function that returns a window size for tree detection.
#'
#' @return A named list of tree metrics:
#' \itemize{
#'   \item \code{n_trees}, \code{trees_per_acre}
#'   \item \code{n_low}, \code{n_low_mid}, \code{n_mid}, \code{n_mid_upper}, \code{n_upper}
#'   \item \code{topo_residual_sd}, \code{topo_entropy}
#' }
#' @export
tree_detection <- function(x, y, z,
window_func = function(x) {
w <- pmax(x, 1)
w <- log1p(w) * 1.5 + 2
w <- pmin(w, 6)
w[is.na(w) | w <= 0] <- 3
return(w)
}
) {
load_graph_deps()
tryCatch({
if (length(x) < 5 || length(z) < 5) {
return(named_zero_metrics('trees'))
}
las_data <- suppressMessages(LAS(data.frame(X = x, Y = y, Z = z)))
trees <- tryCatch({
lidR::locate_trees(las_data, lidR::lmf(ws = window_func))
}, error = function(e) {
return(named_zero_metrics('trees'))
})
if (is.null(trees) || nrow(trees) == 0) {
return(named_zero_metrics('trees'))
}
coords <- sf::st_coordinates(trees)
z_vals <- trees$Z
# True cumulative-from-0.3m counts
n_0_6_1     <- as.numeric(sum(z_vals >= 0.3 & z_vals < 6.1))
n_0_12_1    <- as.numeric(sum(z_vals >= 0.3 & z_vals < 12.1))
n_0_24_1    <- as.numeric(sum(z_vals >= 0.3 & z_vals < 24.1))
n_0_36_1    <- as.numeric(sum(z_vals >= 0.3 & z_vals < 36.1))
n_0_Inf     <- as.numeric(sum(z_vals >= 0.3))
n_gt_6_1    <- as.numeric(sum(z_vals >= 6.1))
n_gt_12_1   <- as.numeric(sum(z_vals >= 12.1))
n_gt_24_1   <- as.numeric(sum(z_vals >= 24.1))
n_gt_36_1   <- as.numeric(sum(z_vals >= 36.1))
zbin <- cut(z_vals,
breaks = c(0.3, 6.1, 12.1, 24.1, 36.1, Inf),
labels = c("low", "low_mid", "mid", "mid_upper", "upper"))
strata <- c("low", "low_mid", "mid", "mid_upper", "upper")
strata_table <- table(zbin)
strata_counts <- setNames(rep(0, length(strata)), strata)
strata_counts[names(strata_table)] <- as.numeric(strata_table)
df <- data.frame(x = coords[, 1], y = coords[, 2], z = z_vals)
residual_sd <- NA_real_
topo_entropy <- NA_real_
if (nrow(df) >= 5) {
tryCatch({
fit <- lm(z ~ splines::bs(x, df = 4) + splines::bs(y, df = 4), data = df)
residuals <- df$z - predict(fit, newdata = df)
residual_sd <- sd(residuals)
h <- hist(residuals, breaks = 10, plot = FALSE)
p <- h$counts / sum(h$counts)
topo_entropy <- -sum(p * log(p + 1e-10))
}, error = function(e) {return(NA_real_)
})
}
n_trees <- nrow(trees)
trees_per_acre <- n_trees / 0.222395
return(list(
n_trees = as.numeric(n_trees),
trees_per_acre = as.numeric(trees_per_acre),
n_low = strata_counts["low"],
n_low_mid = strata_counts["low_mid"],
n_mid = strata_counts["mid"],
n_mid_upper = strata_counts["mid_upper"],
n_upper = strata_counts["upper"],
n_0_6_1 = n_0_6_1,
n_0_12_1 = n_0_12_1,
n_0_24_1 = n_0_24_1,
n_0_36_1 = n_0_36_1,
n_0_Inf = n_0_Inf,
n_gt_6_1 = n_gt_6_1,
n_gt_12_1 = n_gt_12_1,
n_gt_24_1 = n_gt_24_1,
n_gt_36_1 = n_gt_36_1,
topo_residual_sd = residual_sd,
topo_entropy = topo_entropy
))
}, error = function(e) {
return(named_zero_metrics('trees'))
})
}
testing_trees <- pixel_metrics(ctg_norm_clip,~tree_detection(X,Y,Z,
window_func =wf),
res=30)
plot(testing_trees)
plot(testing_trees[[17:18]])
plot(testing_trees[[17]] + testing_trees[[18]])
mat <- cbind(values(testing_trees[[17]]), values(testing_trees[[18]]))
pca <- prcomp(na.omit(mat), center = TRUE, scale. = TRUE)
score <- rep(NA, ncell(topo_residual_sd))
score <- rep(NA, ncell(testing_trees[[17]]))
score[!is.na(values(testing_trees[[17]] )) & !is.na(values(testing_trees[[18]] ))] <- pca$x[,1]
rast() <- score
values(score) <- rast()
score
template <- testing_trees[[17]]
values(score) <- template
terra::values(score) <- template
valid_cells <- !is.na(values(testing_trees[[17]])) & !is.na(values(testing_trees[[18]]))
score[valid_cells] <- pca$x[,1]
template <- testing_trees[[17]]
terra::values(score) <- template
terra::values(template) <- score
names(template) <- "pca_score"
# Plot or write
plot(template, main = "Structural PCA Score")
flight_paths <- read.csv("/mnt/boreas/lidar_download/lidar_areas_paths_2025.csv")
View(flight_paths)
remotes::install_github('joshualerickson/lidar.metrics')
lf <- list.files('/mnt/DataDrive1/data/LIDAR/graph_metrics/')
llsc <- terra::sprc(paste0('/mnt/DataDrive1/data/LIDAR/graph_metrics/', lf))
lf <- list.files('/mnt/DataDrive1/data/LIDAR/graph_metrics/')
llsc <- terra::sprc(paste0('/mnt/DataDrive1/data/LIDAR/graph_metrics/', lf))
lf <- list.files('/mnt/DataDrive1/data/LIDAR/graph_metrics/')
llsc <- terra::sprc(paste0('/mnt/DataDrive1/data/LIDAR/graph_metrics/', lf))
mos <- mosaic(llsc)
writeRaster(mos, '/home/josh.erickson/Documents/projects/vbflood/dev/sf_graph_check.tif')
lf <- list.files('/mnt/DataDrive1/data/LIDAR/graph_metrics/')
llsc <- terra::sprc(paste0('/mnt/DataDrive1/data/LIDAR/graph_metrics/', lf))
mos <- mosaic(llsc)
writeRaster(mos, '/home/josh.erickson/Documents/projects/vbflood/dev/sf_graph_check.tif')
writeRaster(mos, '/home/josh.erickson/Documents/projects/vbflood/dev/sf_graph_check.tif', overwrite=T)
lf <- list.files('/mnt/DataDrive1/data/LIDAR/graph_metrics/')
llsc <- terra::sprc(paste0('/mnt/DataDrive1/data/LIDAR/graph_metrics/', lf))
mos <- mosaic(llsc)
writeRaster(mos, '/home/josh.erickson/Documents/projects/vbflood/dev/sf_graph_check.tif', overwrite=T)
